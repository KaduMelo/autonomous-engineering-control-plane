# autonomous-engineering-control-plane
Durable control plane in Python for an agent that fixes failing CI builds and opens a PR — built on Temporal, resumes after worker crashes without redoing work.
